
package cursos;

import java.util.*;

/**
 * Hay que usar esta clase para guardar los mensajes
 * que se puedan producir en BBDDManager.
 *
 * Se debe hacer directamente con sw.add("Mi mensaje");
 * Por ejemplo:
 * StringWriter result = new StringWriter();
 * result.add("SQL:" + e.getMessage() + ";");
 * return result;
 */
public class StringWriter extends ArrayList<String> {
}